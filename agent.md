# التقرير المرجعي الموثق لمحادثة تطوير إضافة PolyVoice لـ NVDA (مُحدّث حتى 24 يوليو 2026 - الإصدار الذهبي 4.7.3)

---

## 📌 1. الملخص التنفيذي ورؤية المشروع
تمثل هذه الوثيقة التفريغ المباشر والمدقق لكافة الأبحاث الفنية، والمقارنات الهيكلية، والتطورات الكودية، وأحداث المحادثة حتى الوصول للإصدار الذهبي المستقر **PolyVoice 4.7.3** لقارئ الشاشة **NVDA** (معمارية 64-بت AMD64 - NVDA 2026).

الهدف الرئيسي للمشروع هو إنشاء **آلة نطق افتراضية موَزّعة خالصَة (Pure Hybrid Router)** تعمل كمنسق ذكي بين جميع آلات النطق المثبتة على جهازك، تتيح التبديل التلقائي والسلس بين أصوات وآلات النطق حسب لغة النص (عربي، إنجليزي، إلخ)، مع احترام إعدادات كل آلة المستقلة وتفادي تداخل الصوت وقص الجمل بالكامل.

---

## 🧬 2. المعمارية الفنية والحلول الهندسية المبتكرة (PolyVoice vs Multi-Lang vs MultiTTS)

تم تصميم PolyVoice لتتفوق على حلول مثل Multi-Lang من خلال معمارية التبديل على مستوى المحارف، مع إدارة طابور النطق والتزامن المقتبسة من أداء تطبيق MultiTTS في الأندرويد:

```
                           ┌────────────────────────────────────────────────────────┐
                           │               MultiLang Add-on (NVDA)                  │
                           │   ✓ ميزة: الاكتشاف الديناميكي لآلات النطق           │
                           │   ✗ عيب: تداخل الصوت، وتسرّع/قص الجمل بالمنتصف        │
                           │   ✗ عيب: تعثر التقطيع في منتصف الجمل المزدوجة        │
                           └───────────────────────────┬────────────────────────────┘
                                                       │
                                                       │
┌─────────────────────────────────────────┐           │           ┌────────────────────────────────────────┐
│         WorldVoice Add-on (NVDA)        │           ├───►       │        PolyVoice 4.7.3 (الإصدار الذهبي)   │
│  ✓ ميزة: طابور صوتي متسلسل وآمن        ├───────────┤           │  - اكتشاف ديناميكي شامل لكل الآلات     │
│  ✗ عيب: قائمة مغلقة حارمة لـ IBMTTS     │           │           │  - تقطيع جراحي دقيق (Character-Level)  │
└─────────────────────────────────────────┘           │           │  - درع الختم الرقمي (IndexCommand Guard)│
                                                       │           │  - فاصل تفريغ الصوت (Buffer Drain 150ms)│
                                                       │           │  - حماية الإقلاع (Safe Startup Guard)  │
                                                       └────────────────────────────────────────┘
                           ┌───────────────────────────┴────────────────────────────┐
                           │            MultiTTS App (Android Engine)               │
                           │   ✓ ميزة: التبديل السلس والانتقال بدون تداخل       │
                           │   ✓ ميزة: الانتظار الذكي لانتهاء الصوت السابـق        │
                           └────────────────────────────────────────────────────────┘
```

---

## 🔍 3. الأخطاء والتحديات التي وقعنا فيها وكيف تم علاجها

خلال مراحل التطوير والتجربة المباشرة مع المستخدم فريد محمد، واجهنا مجموعة من العقبات التقنية الحادة وتم حلها بنجاح:

### 1️⃣ مشكلة فرض إعدادات الصوت وتراجع NVDA لـ Core Voices
* **السبب:** كان ملف `voiceManager.py` يحتوي على أكواد قديمة موروثة تُجبر الإضافة على البحث عن إعدادات SAPI5 وفرضها على الآلات الأخرى (`_find_arabic_voice_config`). فتسقط الآلات في خطأ التوافق وتتراجع لـ Core Voices.
* **الحل:** تم تجريد `voiceManager.py` بالكامل لتعمل الإضافة كـ **موزع خالص (Pure Router)** يقرأ الأصوات المسجلة في الإعدادات دون مساس بالسرعة أو النبرة أو الخصائص الداخلية التي أعدها المستخدم بنفسه لكل آلة.

### 2️⃣ انهيار واجهة الإعدادات عند الضغط على موافق (AttributeError on `.clear()`)
* **السبب:** في `settingsPanel.py` كانت الدالة `onSave` تحاول استدعاء `.clear()` على قسم الإعدادات `config.conf["PolyVoice"]["bindings"]`. في بيئة NVDA 2026، كائنات الكونفيج لا تدعم `.clear()`.
* **الحل:** استبدال `.clear()` بإعادة تعيين القاموس مباشرة `bindings_section = {}`.

### 3️⃣ تسرب الكلمات الإنجليزية في منتصف الجمل إلى آلة SAPI5 (Lucy)
* **السبب:** الخوارزمية القديمة في `split_sequence` كانت تقطع النص على مستوى "القطع الكبيرة" (Chunks). فإذا بدأت الجملة بالعربية، تُرسل القطعة بأكملها لـ SAPI5، فتقوم Lucy بنطق الكلمات الإنجليزية الموجودة في المنتصف.
* **الحل:** إعادة كتابة `split_sequence` في `languageDetection.py` لتقطيع النص **حرفاً بحرف (Character-Level)** مع استدعاء `unicodedata` لضم علامات الترقيم والأرقام والمسافات للغتها الملتصقة بها، مما أتاح استلال الكلمات الإنجليزية من قلب الجمل العربية وإرسالها لـ IBMTTS.

### 4️⃣ التداخل الصوتي والتلعثم أثناء التصفح السريع بالأسهم
* **السبب:** عند الحركة السريعة، يُرسل NVDA أمر `cancel()`. لكن آلات النطق (مثل SAPI5) تُطلق إشعارات انتهاء قديمة (`synthDoneSpeaking`) بعد إلغائها بملي ثوانٍ. هذه الإشعارات القديمة كانت تخدع الإضافة وتجعلها تظن أن السطر الجديد انتهى فتقوم بتشغيل الصوت اللاتيني فوراً فوق الصوت العربي!
* **الحل المركب:**
  1. **الختم الرقمي (`IndexCommand Guard`):** في كل مرة يتم إرسال مقطع للآلة، نضع في بدايته `IndexCommand(unique_id)`. وإذا ورد إشعار `synthDoneSpeaking` قبل أن يتأكد نظامنا من وصول الآلة لـ `IndexCommand` الخاص بالسطر الحالي، يُعتبر الإشعار "مزيفاً وقديماً" ويتم تجاهله تماماً.
  2. **مهلة تفريغ الصوت (Buffer Drain 150ms):** إضافة تأخير زمني مقداره 150 ملي ثانية حصراً عند التبديل بين آلتين مختلفتين للسماح لكارت الصوت بتفريغ البافر.

### 5️⃣ انهيار NVDA عند الإقلاع (`NVDANotInitializedError: wx.App is not initialized`)
* **السبب:** استخدام `wx.CallAfter` أو `core.callLater` مباشرة داخل `SynthDriver` أدى لانهيار النواة أثناء إقلاع NVDA، لأن NVDA يقوم بنطق عبارة الإقلاع الأولى قبل أن تكتمل تهيئة واجهة المستخدم الرسومية `wx.App`.
* **الحل:** بناء دوال حماية `_safeCallAfter` و `_safeCallLater` تفحص `wx.GetApp()`. إن كان `None` (لحظة الإقلاع الأولى)، تُنفذ الإضافة النطق فوراً وبشكل مباشر دون استخدام `wx` لتفادي الانهيار.

---

## 📐 4. السجل الزمني للإصدارات وتطور الحزم

| الإصدار (Version) | التغييرات والإصلاحات الرئيسية | الحالة |
| :--- | :--- | :---: |
| **v0.1.0 - v4.4.0** | التجارب الأولى، ضبط الهيكل العام وتوافقية NVDA 2026 (AMD64) وإضافة سمة `COM_CLASS`. | ⚠️ غير مستقر |
| **v4.5.0** | إصلاح حفظ الإعدادات في `settingsPanel.py` وإزالة وفرض الخصائص الصوتية يدوياً. | ⚠️ مشاكل تداخل |
| **v4.6.0** | تجريد `voiceManager.py` بالكامل لتصبح الإضافة موزعاً خالصاً لا يتدخل في سرعة أو حدة الآلات. | ⚠️ تسرب الإنجليزية بالمنتصف |
| **v4.7.0** | التقطيع الجراحي المحارفي (Character-Level) واستدلال لغة الحرف عبر `unicodedata` وإضافة طابور النطق. | ⚠️ ارتباك عند الحركة السريعة |
| **v4.7.1** | تطبيق الختم الرقمي `IndexCommand` لتجاهل إشعارات الإلغاء القديمة. | ❌ انهيار عند الإقلاع |
| **v4.7.2** | تجربة استبدال `wx.CallAfter` بـ `core.callLater`. | ❌ انهيار عند الإقلاع |
| **v4.7.3** | **الإصدار الذهبي المستقر:** إضافة دوال الحماية `_safeCallAfter` وتفادي الانهيار عند الإقلاع مع النجاح التام. | **✅ مستقر 100%** |
| **v1.0.0-beta** | **النسخة التجريبية الأولى:** دعم التدويل الكامل (i18n) للتوافق مع لغة NVDA المحددة، إنشاء مستودع GitHub، وإدراج رابط المستودع في `manifest.ini`. | **🚀 متاح على GitHub** |

---

## 📁 5. البنية الشجرية للمشروع المعتمد (`PolyVoice-1.0.0-beta.nvda-addon`)

```
PolyVoice-1.0.0-beta.nvda-addon (Root Level)
├── manifest.ini                         # (version=1.0.0-beta, url=https://github.com/farid5006/PolyVoice)
├── README.md                            # دليل الاستخدام والوصف الرسمي بالإنجليزية لـ GitHub
├── addon.xml                            # التوثيق والتحقق القياسي لـ NVDA
├── __init__.py                          # ملف التهيئة الرئيسي
├── documentation/
│   └── readme.html                      # الدليل والتعليمات
├── globalPlugins/
│   └── PolyVoice/
│       ├── __init__.py                  # واجهة الإعدادات مع دعم التدويل i18n عبر addonHandler
│       └── settingsPanel.py             # لوحة التخصيص متعددة اللغات المترجمة
└── synthDrivers/
    └── PolyVoice/
        ├── __init__.py                  # النواة الرئيسية (تحوي _safeCallAfter و IndexCommand Guard)
        ├── voiceManager.py              # مدير اكتشاف الآلات كـ Router خالص
        ├── languageDetection.py         # التقطيع الدقيق على مستوى المحارف (unicodedata)
        ├── taskManager.py               # مدير صف المهام
        └── voiceInfo.py                 # كائنات بيانات الآلات
```

---

## 🏆 6. التوصيات والنشر على GitHub
- **رابط المستودع الرسمي:** `https://github.com/farid5006/PolyVoice`
- **التدويل (i18n):** تم تفعيل `addonHandler.initTranslation()` واستخدام `_()` لضمان عرض واجهة الإعدادات بلغة NVDA المعينة لدى المستخدم (عربي/إنجليزي/إلخ).
- **رابط المعلومات في NVDA:** حقل `url` داخل `manifest.ini` تم ربطه مباشرة برابط المستودع ليعرضه NVDA لكل مستخدم يفتح "معلومات الإضافة".
- **تصفية التوثيق:** تم إزالة أي إشارة لاختصارات مفاتيح من ملف `README.md` والتكويد للتأكيد على الوصول المباشر عبر قائمة تفضيلات NVDA (`Preferences ➔ Settings ➔ PolyVoice`).

---
*تم إعداد وتحديث هذا التقرير الشامل والموثق بفضل الله ثم العمل الدؤوب في ملف المشروع `D:\documents\programming corse\PolyVoice\agent.md` ليصبح المرجع الأول والكامل لأي تطوير مستقبلي.*
